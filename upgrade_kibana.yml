- name: Upgrade kibana
  hosts: kibana
  serial: 1
  become: true
  roles:
    - role: yum_update_and_check_available_version
      vars:
        package_name: kibana.x86_64
    #- role: upgrade_elastic_tool
    #  vars:
    #    elastic_component: kibana